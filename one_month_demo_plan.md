# AlbumMind 一个月 Demo 简版计划

> 落地说明：最终选择在 RivuMusic 的 React + Tauri + 网易云 API 基础上实现，因此不再重建 Next.js、Spotify 和 Supabase 基础设施。产品目标保持不变：专辑优先推荐、收藏闭环、外部平台入口，以及由 AI 解释推荐而不是让 AI 直接决定推荐。

## 1. 项目定位先定清楚

这个项目不要做成“完整音乐播放器”，也不要做成本地音乐库管理工具。更适合作为实习项目的定位是：

> 一个以专辑为主体的 AI 音乐推荐与收藏 Web App。

用户不是来这里替代网易云、Spotify 或 Apple Music 播歌，而是用它来发现专辑、理解专辑、收藏专辑和歌曲，并跳转到外部音乐软件播放。

这样做的好处是范围小、完成度高、面试时也容易讲：你做的是“音乐推荐产品 + 外部 API 聚合 + AI 应用”，而不是一个很难做完整的播放器。

## 2. 你需要决定的关键问题

### 决策 1：用哪个音乐 API 做主数据源？

建议：**Spotify 为主，Last.fm / MusicBrainz 为辅。**

Spotify 适合做登录、搜索、专辑封面、歌曲列表、外部播放链接。它的数据质量好，Demo 效果也漂亮。

但不要依赖 Spotify 的推荐接口，因为推荐接口可用性和政策限制不稳定。推荐逻辑可以自己做一层简单规则：根据用户收藏的艺术家、歌曲、专辑标签，去找相似艺术家和热门专辑。

Last.fm 适合补推荐候选，比如相似艺术家、热门专辑、风格标签。MusicBrainz 适合补更标准的专辑元数据，但第一版不要接太深。

### 决策 2：要不要做播放？

建议：**不做完整播放，只做外部跳转和可选试听。**

完整播放会遇到版权、账号、SDK、Premium 权限等问题，一个月内很容易拖垮进度。Demo 里只需要做到：

- 专辑详情页有 Spotify / 外部平台播放按钮
- 如果接口返回 preview_url，就展示 30 秒试听
- 没有试听就不展示，不强求

这样面试时也说得通：你的重点是推荐和收藏，不是替代播放器。

### 决策 3：AI 到底做什么？

建议：**AI 做解释，不做黑盒推荐。**

也就是说，推荐候选先由规则和 API 生成，AI 只负责把结果讲得更像人：

- 为什么推荐这张专辑
- 这张专辑适合什么场景听
- 先听哪几首
- 它和用户喜欢的音乐有什么相似点
- 它有什么不同，为什么值得尝试

这样最稳。你不用训练模型，也不用做复杂算法，但能体现 AI 应用能力。

### 决策 4：收藏对象是歌曲还是专辑？

建议：**两个都做，但界面以专辑为主。**

用户可以收藏歌曲，因为这符合大多数人的习惯；但系统推荐时要把歌曲往专辑聚合。

比如用户收藏了一首歌，产品应该引导他看到：

- 这首歌来自哪张专辑
- 这张专辑为什么值得完整听
- 这位艺术家还有哪些相似专辑

这就是你的差异化：不是只推荐单曲，而是从单曲回到专辑。

### 决策 5：技术栈怎么选？

建议：

- Next.js
- TypeScript
- Tailwind CSS
- Supabase
- Spotify OAuth
- Last.fm API
- 一个大模型 API，例如 DeepSeek、通义千问或 OpenAI

不要做桌面端，也不要一开始做移动端。Web App 最容易部署、展示和写进简历。

## 3. 一个月开发节奏

### 第 1 周：跑通数据

目标是看到真实音乐数据，而不是纠结设计。

完成：

- 初始化项目
- 接入 Spotify 搜索
- 能搜索歌曲、专辑、艺术家
- 能展示专辑卡片
- 建好收藏相关数据库表

### 第 2 周：做出产品闭环

完成：

- 专辑详情页
- 歌曲列表
- 收藏歌曲
- 收藏专辑
- 收藏页
- 外部播放跳转

到这里项目已经能演示基本流程。

### 第 3 周：加入推荐和 AI

完成：

- 根据收藏生成推荐候选
- 用 Last.fm 找相似艺术家和热门专辑
- 做简单排序
- AI 生成推荐理由和听前导览

这是项目的亮点周。

### 第 4 周：打磨和包装

完成：

- UI 简洁统一
- 加载态、空状态、错误态
- 部署到 Vercel
- 写 README
- 准备演示脚本和截图

不要在第 4 周加大功能，重点是让它像一个完成的项目。

## 4. 最终 Demo 只需要证明这件事

用户收藏几首歌或几张专辑后，系统能推荐一组“值得完整听的专辑”，并用 AI 解释推荐原因。

如果这个闭环顺畅，你的项目就成立了。

保底功能只有五个：

1. 搜索音乐
2. 查看专辑详情
3. 收藏歌曲/专辑
4. 推荐专辑
5. AI 解释推荐理由

这五个做好，比做十几个半成品功能更适合实习项目。
